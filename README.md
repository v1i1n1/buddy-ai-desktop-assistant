````markdown

# Buddy AI Assistant

Buddy AI Assistant is a local AI-powered desktop assistant built using Python, Llama 3.2, Ollama, Streamlit, voice recognition, and Windows automation tools.

The assistant can understand natural-language commands, convert them into structured actions, and safely execute approved operations on a Windows laptop.

## Features

- Local Llama 3.2 inference using Ollama
- Voice-based interaction
- Wake phrase: "Hey Buddy"
- Text-to-speech responses
- Natural-language command understanding
- Structured JSON-based intent generation
- Multi-step command execution
- Desktop application control
- Google search and website opening
- Keyboard automation
- Screenshot capture
- Volume control
- Battery status
- CPU and RAM monitoring
- System information
- Short-term conversation context
- Streamlit-based user interface
- Start and Stop controls
- Live assistant status
- Conversation history
- Animated voice-assistant waveform

## Supported Commands

Examples:

- "Hey Buddy"
- "Open Notepad"
- "Open Calculator"
- "Open Camera"
- "Open Task Manager"
- "Open Chrome"
- "Open Downloads"
- "Open Documents"
- "Search Google for AWS Bedrock"
- "Open Notepad and type Hello Buddy"
- "Calculate 6 plus 5"
- "Increase the volume"
- "Decrease the volume"
- "Mute the laptop"
- "Take a screenshot"
- "What is my battery percentage?"
- "What is my CPU usage?"
- "How much RAM am I using?"
- "Show my system information"
- "Close Notepad"
- "Close Calculator"
- "Close Chrome"

## Architecture

User Voice / Text

↓

Speech Recognition

↓

Llama 3.2 through Ollama

↓

Structured JSON Intent

↓

Python Intent Executor

↓

Allowlisted Tools

↓

Windows Desktop Actions

↓

Text-to-Speech Response

↓

Streamlit Conversation UI

## Technology Stack

- Python 3.11
- Llama 3.2
- Ollama
- Streamlit
- SpeechRecognition
- PyAudio
- pyttsx3
- PyAutoGUI
- psutil
- Requests
- HTML / CSS
- Python Threading

## Project Structure

```text
buddy-ai-desktop-assistant/
│
├── tools/
│   ├── __init__.py
│   └── app_tools.py
│
├── assistant_service.py
├── intent_parser.py
├── main.py
├── speech_output.py
├── ui.py
├── voice_input.py
├── requirements.txt
├── .gitignore
└── README.md
````

## How It Works

Buddy uses Llama 3.2 as the natural-language understanding layer.

For example:

User:

```text
Open Notepad and type Hello Buddy
```

Llama converts the request into structured JSON:

```json
{
  "type": "command",
  "steps": [
    {
      "action": "open_app",
      "application": "notepad"
    },
    {
      "action": "type_text",
      "text": "Hello Buddy"
    }
  ]
}
```

The Python execution layer validates these actions and calls only approved functions.

The LLM does not directly execute arbitrary operating-system commands.

## Safety Design

The project uses an allowlisted tool-execution architecture.

Instead of directly executing commands generated by the LLM:

```text
LLM
↓
Structured Intent
↓
Validation
↓
Approved Python Tool
↓
Windows Action
```

This reduces the risk of unintended operating-system commands.

## Installation

Clone the repository:

```bash
git clone <your-repository-url>
cd buddy-ai-desktop-assistant
```

Create a virtual environment:

```bash
python -m venv venv
```

On Windows PowerShell:

```powershell
.\venv\Scripts\Activate.ps1
```

Install dependencies:

```bash
pip install -r requirements.txt
```

## Install Ollama

Install Ollama separately on your system.

Pull Llama 3.2:

```bash
ollama pull llama3.2
```

Verify the model:

```bash
ollama list
```

## Run the Application

Start the Streamlit interface:

```bash
streamlit run ui.py
```

Then click:

```text
START BUDDY
```

and say:

```text
Hey Buddy
```

## Current Limitations

* Llama inference performance depends on local hardware.
* Current speech recognition uses Google's speech recognition service and requires internet connectivity.
* Desktop automation is currently designed for Windows.
* Chrome behavior may depend on the local Chrome profile configuration.
* The animated waveform represents assistant state and is not yet driven by actual microphone amplitude.

## Future Enhancements

* Local speech-to-text using Faster Whisper
* Real-time microphone waveform
* Improved conversational memory
* LangGraph-based agent orchestration
* More desktop tools
* File search and document operations
* Application-specific automation
* Configurable wake word
* Persistent memory
* Improved security confirmations for sensitive actions

## Disclaimer

This project is intended for learning, experimentation, and local desktop automation. Use system-control functionality carefully and only on devices you own or are authorized to control.

````
